<resources>
    <!-- Base application theme. -->
    <style name="Theme.NMedia" parent="Theme.MaterialComponents.DayNight.NoActionBar">
        <!-- Primary brand color. -->
        <item name="colorPrimary">@color/purple_500</item>
        <item name="colorPrimaryVariant">@color/purple_700</item>
        <item name="colorOnPrimary">@color/white</item>
        <!-- Secondary brand color. -->
        <item name="colorSecondary">@color/teal_200</item>
        <item name="colorSecondaryVariant">@color/teal_700</item>
        <item name="colorOnSecondary">@color/black</item>
        <!-- Status bar color. -->
        <item name="android:statusBarColor">?attr/colorPrimaryVariant</item>
        <!-- Customize your theme here. -->
    </style>

    <style name="Widget.App.PostButton" parent="Widget.MaterialComponents.Button.TextButton.Icon">
        <item name="cornerRadius">24dp</item>
        <item name="android:minWidth">@dimen/icon_button_min_size</item>
    </style>

    <style name="Widget.App.PostButton.Like">
        <item name="android:checkable">true</item>
        <item name="icon">@drawable/ic_like_24</item>
        <item name="iconTint">@color/like_tint</item>
    </style>

    <style name="Widget.App.PostButton.Share">
        <item name="icon">@drawable/ic_share_24</item>
        <item name="iconTint">@color/like_tint</item>
        <item name="android:textColor">?colorControlNormal</item>
    </style>

    <style name="Widget.App.Button.OutlinedButton.IconOnly" parent="Widget.MaterialComponents.Button.OutlinedButton">
        <item name="iconPadding">0dp</item>
        <item name="android:insetTop">0dp</item>
        <item name="android:insetBottom">0dp</item>
        <item name="android:paddingLeft">12dp</item>
        <item name="android:paddingRight">12dp</item>
        <item name="android:minWidth">48dp</item>
        <item name="android:minHeight">48dp</item>
    </style>

    <style name="Widget.App.Button.Menu" parent="Widget.App.Button.OutlinedButton.IconOnly">
        <item name="icon">@drawable/ic_more_vert_24</item>
        <item name="iconTint">?colorControlNormal</item>
    </style>
</resources>
